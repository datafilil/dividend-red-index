# 中证红利全收益指数周报（dividend-red-index）

## 访问与可见性（2026-09-24 封闭决策）

本项目为**个人自用、封闭**工具，**不对外公开**：

- 仓库已设为 **Private**（Settings → Change visibility → Make private）。
- **不再发布公开 Pages 页**：`weekly-report.yml` 已移除「同步到 Pages 根目录」步骤，
  旧公开页产物 `index.html` / `site/` 已从仓库删除并加入 `.gitignore`。
- 每周自动化仍在跑（GitHub Actions，仅提交报告与缓存增量到本私有仓库），
  **报告仅仓库所有者可见，不再有任何人可读的公开 URL**。
- 如需彻底本地化（删掉云端自动化、本机手动跑），删除 `.github/workflows/weekly-report.yml` 即可。

---

fix+feat: 趋势图定版 + 成分股模块（相对 a78066a 的全部变更）

【趋势图定版】
1. σ 改用稳健 MAD 估计（中位数绝对偏差×1.4826）：经典σ被2015/2018/2024
   崩盘段极端残差撑大约32%（|残差|最大30天贡献平方和21%），MAD版更贴近
   常态散布，±1.5σ带更灵敏。
2. 两图起点统一为 2016-01-01（40日收益差图 + loglog二阶拟合图），
   2016起的样本同处2013-07股息率加权改革后的框架内。
   新读数：σ(MAD)=0.0584，±1.5σ带半宽≈9.1%，当前偏离+3.5%带内。
3. 灰色虚线竖线标注 2022-12-12 / 2025-10-13 两次编制方案修订日，
   作结构性断点参考，不剔除。

【新增成分股模块】
4. 当前样本概览（官网 closeweight 权重文件，月末口径）：
   100只成分、前十大权重表、股息率加权均值（4.04%，与官网D/P2=4.12%互证）。
   个股价息率 = 最近一个已实施分红的会计年度（中报+年报）每股税前分红 ÷ 现价，
   自算自东财分红明细（修正了跨年滚动窗口漂移、未除息中期分红误归属两个bug；
   行情接口f115字段对小盘股失真严重，已弃用）。
5. 行业权重分布（东财行业口径，37行业折叠表）：注明编制方案无行业权重
   条款，行业集中（煤炭22.45%+银行22.29%）为股息率选样的自然涌现结果。
6. 定期调整预告（2026-12-14生效，规则推算非官方名单）：
   - 缓冲区条款官方原文（股息率>0.5% / 市值成交前90% / 支付率0~1）
   - 剔除候选20只（按公告惯例20进20出取满额，≤0.5%红标硬预警）
   - 纳入候选Top20（全市场规则化筛选：连续3年分红+支付率0~1+市值前80%，
     三年平均股息率排名；结果缓存7天，扫描失败降级读旧缓存，不中断主流程）
7. 近五年样本调整史（2021-2025，公告/生效日+进出名单，折叠展示）。

【展示形态】
8. 概览/预告两区块改为 <details> 默认收起、点击展开，纳入候选为二级折叠。

【健壮性】
9. 成分股/股息率/行业/纳入候选各接口失败均自动降级（读缓存或隐藏区块），
   不中断主报告生成；workflow 与 requirements.txt 无需改动。

## 框架范围锁定（2026-09-20 决策）

围绕「中证指数官网还能挖掘什么数据框架」共评估 6 个方向，最终取舍如下：

| # | 框架 | 决策 | 状态 |
|---|------|------|------|
| 1 | 估值温度计（PE/股息率/EP 等历史序列） | 不需要 | ❌ 已移除（2026-09-20 晚）：周报不再含温度计段与 PE/EP 估值卡 |
| 2 | （用户明确不需要） | 不需要 | ❌ 未建 |
| 3 | （用户明确不需要） | 不需要 | ❌ 未建 |
| 4 | 成分质量 / 剔除风险预警（延伸 20进20出） | 需要 | ✅ 已建：双维度 股息率+连续分红，触发数 N/20 汇总，风险标红 |
| 5 | 指数纳入效应事件研究（可交易事件） | 不需要 | ❌ 已移除（2026-09-20 晚）：周报折叠段删除；event_study.py 保留可作独立分析工具单独运行 |
| 6 | （用户明确不需要） | 不需要 | ❌ 未建 |

- ① 估值温度计、⑤ 指数纳入效应均于 2026-09-20 晚从周报移除；当前周报仅保留 ④ 成分质量/剔除风险预警（＋ 原有价格趋势 / 破MA500 / 长期拟合核心段）。
- ② / ③ / ⑥ 为用户 2026-09-20 明确「不需要」，故不实现、不占用周报篇幅；其具体提案定义以当时对话为准，本文件仅记录取舍结论。
